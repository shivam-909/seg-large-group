import { render, unmountComponentAtNode} from '@testing-library/react';
import TextInputBoxWithIcon from '../Components/LoginPage/TextInputBoxWithIcon';


describe ('TextInputBox component', () => {
    it ('should render component on the screen', () => {
        expect(true).toBeTruthy();
    }); 

    it ('render icons on the screen', async () => {

    })









// let container = null;
// beforeEach(() => {
//     container = document.createElement('div');
//     document.body.appendChild(container);
// });

// afterEach(() => {
//     unmountComponentAtNode(container);
//     container.remover()
//     container= null;
// });


// it ('should render component on the screen', () => {
//         expect(true).toBeTruthy();
//     )};

//     it ('icon changes from show icon to hide icon when clicked', () => {
//         render(<TextInputBoxWithIcon{...props}/>);

//         const icons = screen.getByPlaceholderValue('');

//     )};


    // it ('renders message on the screen', () => {
    //     render (<TextInputBox/>);
    //     expect(message).toBeInTheDocument();
    // });
